# GA208
## Devlogs
### W1
W1 Exercise

Q1
10

Q2
2

Q3
Prints "hello world" in the debug log in the unity editor every frame since it's in the update function. 

Q4
Monobehavior 

Q5
Converts 10 into a string and replaced any instance of 'x' with it. The debug log will read "x = 10"

Q6
10 is "int x" within the 'private void PrintMessage (int x)' line. It is an interger, but is converted into a string in the devlog. '(x = " + x);' is the debug log string, the line of text the debug log will display in the unity editor.

Q7
The object is transforming in the player direction but with no speed nor input. 

Q8
Transform.x?
Transform.y?

### W2